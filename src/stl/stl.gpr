library project Stl is
   for Languages use ("Ada", "C");
   for Library_Name use "Stl";
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Kind use "Static";

   package Binder is
      for Default_Switches ("Ada") use ("-E");
   end Binder;

end Stl;
